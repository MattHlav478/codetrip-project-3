// require all models
// using Auth from Apollo? tokens? if so, require

const resolvers = {
    Query: {},
    Mutation: {},
};
// inside of resolvers, Query and Mutation
// inside of each, add the specific named Q or M according to those sketched out in typeDefs

module.exports = resolvers;
