const faker = require("faker");

// const db = require connection to config/connection.js
// require models

// look online for using faker to create seed data, or else refer to deep-thoughts example

db.once("open", async () => {});
